services:
  strava-heatmap-proxy:
    build: .
    container_name: strava-heatmap-proxy
    ports:
      - "8080:8080"
    volumes:
      - "${USERPROFILE}/.config/strava-heatmap-proxy:/config:ro"
    command: ["--port", "8080", "--cookies", "/config/strava-cookies.json", "--verbose"]
    restart: unless-stopped
